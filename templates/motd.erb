This server belongs to K-Gohil Inc. Any un-authorise access to this box will be considered as a violation of Cyber Laws could result into legal actions.
        HOSTNAME        : <%= node['hostname'] %>
        IPADDRESS       : <%= node['ipaddress'] %>
        MEMORY          : <%= node['memory']['total'] %>
        CPU             : <%= node['cpu']['0']['mhz'] %>
